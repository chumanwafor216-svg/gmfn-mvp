# GMFN Screenshot Evidence Checklist (Visa Pack)

Capture each item with date/time visible if possible.

## Required Set (Phase 1)
1. Community → Invite card (copy link + WhatsApp share)
2. Join success → member appears in clan list
3. Loans → Incoming Guarantor Requests (approve/decline + reason + note)
4. Loans → My Guarantees (exposure transparency)
5. TrustSlip → Share with Merchant block
6. TrustSlip Verify → VALID / NOT VALID
7. Admin → Recent TrustEvents feed (reason + note visible)
8. Trust → /trust/me output (trust_score + last_change)
9. Trust → /trust/me/why output (explainability timeline)
10. Loan → /loans/{loan_id}/evidence-snapshot JSON (coverage + repayments + events)

## Optional (Strong Add-ons)
- Evidence pack PDF endpoint output
- Trust timeline PDF
- Bank reconciliation endpoints showing deterministic matching (Phase 1 bank)